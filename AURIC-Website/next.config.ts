import type { NextConfig } from 'next';
const config:NextConfig={output:'export',poweredByHeader:false,images:{unoptimized:true},productionBrowserSourceMaps:false,trailingSlash:true};
export default config;
