// Local verification only; production deploys out/ without this server.
import http from 'node:http';import fs from 'node:fs';import path from 'node:path';
const root=path.resolve('out'),headers=JSON.parse(fs.readFileSync('.sites-runtime/security-headers.json','utf8'));
// Internal QA runs over HTTP; retain all production directives except transport upgrade.
headers['Content-Security-Policy']=headers['Content-Security-Policy'].replace('; upgrade-insecure-requests','');
const mime={'.html':'text/html; charset=utf-8','.css':'text/css','.js':'application/javascript','.webp':'image/webp','.png':'image/png','.svg':'image/svg+xml','.woff':'font/woff','.woff2':'font/woff2','.xml':'application/xml','.txt':'text/plain'};
http.createServer((req,res)=>{let p;try{p=decodeURIComponent(new URL(req.url,'http://localhost').pathname);}catch{res.writeHead(400);res.end('Bad request');return;}let file=path.resolve(root,'.'+p);if(!file.startsWith(root+path.sep)&&file!==root){res.writeHead(404);res.end('Not found');return;}if(p.split('/').some(s=>s.startsWith('.'))||p==='/_headers'){res.writeHead(404,headers);res.end('Not found');return;}if(fs.existsSync(file)&&fs.statSync(file).isDirectory())file=path.join(file,'index.html');const found=fs.existsSync(file)&&fs.statSync(file).isFile();if(!found)file=path.join(root,'404.html');res.writeHead(found?200:404,{...headers,'Content-Type':mime[path.extname(file)]||'application/octet-stream','Cache-Control':p.startsWith('/_next/static/')?'public, max-age=31536000, immutable':'no-cache'});fs.createReadStream(file).pipe(res);}).listen(4173,'0.0.0.0',()=>console.log('Static verification server ready'));
